<%= form_tag( books_path, :method=>'get', :class=>'form-horizontal', :id=>'isbnForm') do %>
    <div class="tab-pane active col-xs-offset-1 col-xs-10" id="bookname">
      <div class="form-group">
        <%= text_field_tag :search, nil, :placeholder=>'Search', :class=>'form-control index-book-search ', :id=>"bookname" %>
        <i class="glyphicon glyphicon-search book-search-icon"></i>
      </div>
    </div>
<% end %>